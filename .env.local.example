# Local development (Mac)
# Backend-altezza running locally:
#   http://localhost:3022/api/responseAltezza
HOST_NAME=http://localhost:3022/api/responseAltezza

# Optional: if you want the backend to return absolute URLs for images
# (otherwise it can return relative paths and you can prepend in UI)
# PUBLIC_BASE_URL=http://localhost:3022
